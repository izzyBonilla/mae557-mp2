## Lid-driven Cavity Flow Solver
Uses 2nd order central difference and forward Euler to solve the 2D flow in a box with an oscillating lid. Requires eigen. Compile with `make` from the `src` directory.
